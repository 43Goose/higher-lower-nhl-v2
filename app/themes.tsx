'use client';
import { createTheme } from "@mui/material";

export const mainTheme = createTheme({
    palette: {
        primary: {
            main: '#0F172A',
        },
        secondary: {
            main: '#06B6D4',
        },
    },
});